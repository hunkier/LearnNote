---
global:
  user: "tidb"
  ssh_port: 22
  deploy_dir: "/tidb/dm/deploy"
  data_dir: "/tidb/dm/data"
  # arch: "amd64"

master_servers:
  - host: localhost

worker_servers:
  - host: localhost

monitoring_servers:
  - host: localhost
    port: 9091

grafana_servers:
  - host: localhost
    port: 3001

alertmanager_servers:
  - host: localhost
    web_port: 9096
    cluster_port: 9097
  